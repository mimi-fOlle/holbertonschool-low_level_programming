#include <stdio.h>
#include "main.h"

/**
*
*
*
*/

void times_table(void)
{
	int n;
	for (n = 0; n <= 9; n++)
	{
		_putchar(n);
		
		if (n != 9)
		{
		_putchar(44);
		_putchar(32);
		}
	}
	_putchar(10);
}

	int n=0; j;
	while (n <= 9)
	{
		j=1;
		while (j <= 9)
		{
			_putchar((n^
			n++;
			j++;
